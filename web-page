import React, { useState, useEffect, useRef } from 'react';
import { 
  Terminal, 
  Cpu, 
  Code2, 
  Send, 
  User, 
  Sparkles, 
  Menu, 
  X, 
  Github, 
  Linkedin, 
  Mail, 
  Zap,
  Layout,
  MessageSquare,
  Play,
  Monitor,
  Folder,
  FileText,
  FileJson,
  FileCode,
  Settings,
  Search,
  ChevronRight,
  ChevronDown,
  Command
} from 'lucide-react';

// --- Assets & Styles ---
// Using Tailwind CSS (assumed available)

const Portfolio = () => {
  const [activeFile, setActiveFile] = useState('README.md');
  const [isSidebarOpen, setSidebarOpen] = useState(true);
  const [isChatOpen, setChatOpen] = useState(true);
  
  // Mobile check
  useEffect(() => {
    const handleResize = () => {
      if (window.innerWidth < 768) {
        setSidebarOpen(false);
        setChatOpen(false);
      } else {
        setSidebarOpen(true);
        setChatOpen(true);
      }
    };
    window.addEventListener('resize', handleResize);
    handleResize();
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  const files = [
    { name: 'README.md', icon: <FileText size={15} className="text-blue-400" />, type: 'markdown' },
    { name: 'projects.json', icon: <FileJson size={15} className="text-yellow-400" />, type: 'json' },
    { name: 'profile.config.ts', icon: <FileCode size={15} className="text-cyan-400" />, type: 'typescript' },
    { name: 'contact.sh', icon: <Terminal size={15} className="text-green-400" />, type: 'shell' },
  ];

  return (
    <div className="flex flex-col h-screen bg-[#09090b] text-gray-300 font-sans overflow-hidden selection:bg-cyan-500/30">
      
      {/* Top Bar (Title Bar) */}
      <div className="h-10 border-b border-white/5 bg-[#0c0c0e] flex items-center justify-between px-4 select-none">
        <div className="flex items-center gap-3">
          <div className="flex gap-2 group">
             <div className="w-3 h-3 rounded-full bg-red-500/20 group-hover:bg-red-500 transition-colors" />
             <div className="w-3 h-3 rounded-full bg-yellow-500/20 group-hover:bg-yellow-500 transition-colors" />
             <div className="w-3 h-3 rounded-full bg-green-500/20 group-hover:bg-green-500 transition-colors" />
          </div>
          <span className="ml-4 text-xs font-mono text-gray-500">FEI.DESIGN — {activeFile}</span>
        </div>
        
        <div className="flex items-center gap-4 text-xs font-mono text-gray-500">
          <div className="flex items-center gap-2 px-3 py-1 bg-white/5 rounded-full">
            <div className="w-2 h-2 rounded-full bg-green-500 animate-pulse" />
            <span className="text-green-500">SYSTEM ONLINE v2.4.0</span>
          </div>
          <span className="hidden md:inline">RAM: 64GB</span>
        </div>
      </div>

      {/* Main IDE Area */}
      <div className="flex-1 flex overflow-hidden">
        
        {/* Left Sidebar (Explorer) */}
        <div className={`${isSidebarOpen ? 'w-64' : 'w-0'} bg-[#0c0c0e] border-r border-white/5 flex flex-col transition-all duration-300 overflow-hidden shrink-0`}>
          <div className="p-3 text-xs font-bold text-gray-400 tracking-wider flex justify-between items-center">
            <span>EXPLORER</span>
            <Settings size={14} className="hover:text-white cursor-pointer" />
          </div>
          
          <div className="flex-1 overflow-y-auto">
            <div className="px-2">
              <div className="flex items-center gap-1 text-gray-400 py-1 cursor-pointer hover:text-white">
                <ChevronDown size={14} />
                <Folder size={14} className="text-purple-400" />
                <span className="text-sm font-bold">portfolio-v2</span>
              </div>
              
              <div className="pl-4 mt-1 space-y-1">
                {files.map(file => (
                  <button
                    key={file.name}
                    onClick={() => setActiveFile(file.name)}
                    className={`flex items-center gap-2 w-full px-2 py-1.5 rounded text-sm transition-colors ${activeFile === file.name ? 'bg-cyan-500/10 text-cyan-400' : 'text-gray-400 hover:bg-white/5 hover:text-white'}`}
                  >
                    {file.icon}
                    <span className="font-mono text-xs">{file.name}</span>
                  </button>
                ))}
              </div>
            </div>
          </div>

          {/* User Profile Bottom */}
          <div className="p-4 border-t border-white/5">
             <div className="flex items-center gap-3">
               <div className="w-8 h-8 rounded bg-gradient-to-br from-cyan-500 to-blue-600 flex items-center justify-center text-black font-bold text-xs">FD</div>
               <div className="flex flex-col">
                 <span className="text-xs font-bold text-white">Fei Design</span>
                 <span className="text-[10px] text-green-400 flex items-center gap-1">
                   <span className="w-1.5 h-1.5 rounded-full bg-green-500" /> Available
                 </span>
               </div>
             </div>
          </div>
        </div>

        {/* Center Editor Area */}
        <div className="flex-1 flex flex-col min-w-0 bg-[#09090b] relative">
           
           {/* Tab Bar */}
           <div className="h-9 bg-[#09090b] border-b border-white/5 flex items-center overflow-x-auto no-scrollbar">
             {files.map(file => (
               <button 
                 key={file.name}
                 onClick={() => setActiveFile(file.name)}
                 className={`h-full px-4 flex items-center gap-2 text-xs border-r border-white/5 min-w-fit ${activeFile === file.name ? 'bg-[#09090b] text-white border-t-2 border-t-cyan-500' : 'bg-[#0c0c0e] text-gray-500 hover:bg-[#09090b]'}`}
               >
                 {file.icon}
                 {file.name}
                 {activeFile === file.name && <X size={12} className="ml-2 hover:text-red-400" />}
               </button>
             ))}
           </div>

           {/* Breadcrumbs */}
           <div className="h-8 flex items-center px-4 gap-2 text-xs text-gray-500 border-b border-white/5">
             <span>portfolio-v2</span>
             <ChevronRight size={12} />
             <span>src</span>
             <ChevronRight size={12} />
             <span className="text-gray-300">{activeFile}</span>
           </div>

           {/* Editor Content Scroller */}
           <div className="flex-1 overflow-y-auto custom-scrollbar p-8 md:p-12 relative">
              {/* Background Glow */}
              <div className="absolute top-0 right-0 w-[500px] h-[500px] bg-cyan-500/5 blur-[120px] rounded-full pointer-events-none" />
              
              <div className="max-w-4xl mx-auto relative z-10 animate-fade-in-up">
                {activeFile === 'README.md' && <ReadmeContent setActiveFile={setActiveFile} />}
                {activeFile === 'projects.json' && <ProjectsContent />}
                {activeFile === 'profile.config.ts' && <AboutContent />}
                {activeFile === 'contact.sh' && <ContactTerminal />}
              </div>
           </div>

           {/* Mobile Menu Toggles */}
           <div className="md:hidden absolute bottom-4 left-4 right-4 flex justify-between z-50">
              <button onClick={() => setSidebarOpen(!isSidebarOpen)} className="p-2 bg-[#1e1e1e] border border-white/10 rounded-lg text-white shadow-lg">
                <Menu size={20} />
              </button>
              <button onClick={() => setChatOpen(!isChatOpen)} className="p-2 bg-cyan-600 border border-cyan-400 rounded-lg text-white shadow-lg">
                <MessageSquare size={20} />
              </button>
           </div>
        </div>

        {/* Right Sidebar (AI Chat) */}
        <div className={`${isChatOpen ? 'w-80 md:w-96' : 'w-0'} bg-[#0c0c0e] border-l border-white/5 flex flex-col transition-all duration-300 shrink-0 absolute md:relative right-0 h-full z-40 shadow-2xl md:shadow-none`}>
          <div className="h-10 border-b border-white/5 flex items-center justify-between px-4 bg-[#0c0c0e]">
             <span className="text-xs font-bold text-cyan-400 flex items-center gap-2">
               <Sparkles size={14} /> NOVA AI ASSISTANT
             </span>
             <button onClick={() => setChatOpen(false)} className="md:hidden text-gray-400">
               <X size={16} />
             </button>
          </div>
          
          <AIChatInterface />
        </div>

      </div>

      {/* Status Bar */}
      <div className="h-6 bg-cyan-950/20 border-t border-cyan-500/20 flex items-center justify-between px-3 text-[10px] text-cyan-400 font-mono select-none">
        <div className="flex items-center gap-4">
          <div className="flex items-center gap-1">
             <Code2 size={10} />
             <span>master*</span>
          </div>
          <div className="flex items-center gap-1 text-gray-400">
             <X size={10} className="text-red-400" />
             <span>0 errors</span>
          </div>
          <div className="flex items-center gap-1 text-gray-400">
             <div className="w-2 h-2 rounded-full border border-yellow-400" />
             <span>0 warnings</span>
          </div>
        </div>
        
        <div className="flex items-center gap-4">
           <span>Ln 14, Col 32</span>
           <span>UTF-8</span>
           <span>TypeScript JSX</span>
           <div className="flex items-center gap-1 text-white animate-pulse">
             <div className="w-1.5 h-1.5 rounded-full bg-cyan-400" />
             AI Connected
           </div>
        </div>
      </div>

    </div>
  );
};

// --- CONTENT COMPONENTS ---

const ReadmeContent = ({ setActiveFile }) => (
  <div className="prose prose-invert prose-p:text-gray-400 prose-headings:text-gray-200 max-w-none">
    <div className="mb-8 pb-8 border-b border-white/10">
      <h1 className="text-5xl md:text-6xl font-black text-white mb-4 tracking-tight">
        Principal <span className="text-cyan-400">Design Manager</span> <br/>
        @ Microsoft.
      </h1>
      <p className="text-xl text-gray-400 leading-relaxed max-w-2xl">
        Fei is a Principal Design Manager at Microsoft, working on creating the best developer experience with AI in Microsoft Fabric.
      </p>
    </div>

    <div className="grid md:grid-cols-2 gap-4 mb-12">
      <div className="p-6 rounded-xl bg-[#131315] border border-white/5 hover:border-cyan-500/30 transition-all cursor-pointer group" onClick={() => setActiveFile('projects.json')}>
        <div className="flex items-center gap-3 mb-3">
          <Cpu className="text-purple-400 group-hover:scale-110 transition-transform" />
          <h3 className="text-lg font-bold text-white m-0">Full Stack Core</h3>
        </div>
        <p className="text-sm text-gray-500 m-0 font-mono">
          Next.js, TypeScript, Node.js, Postgres, Vector DBs.
        </p>
      </div>

      <div className="p-6 rounded-xl bg-[#131315] border border-white/5 hover:border-cyan-500/30 transition-all cursor-pointer group" onClick={() => setActiveFile('projects.json')}>
        <div className="flex items-center gap-3 mb-3">
          <Layout className="text-cyan-400 group-hover:scale-110 transition-transform" />
          <h3 className="text-lg font-bold text-white m-0">UX/UI Design</h3>
        </div>
        <p className="text-sm text-gray-500 m-0 font-mono">
          Figma, Framer, Tailwind, Three.js, Spline.
        </p>
      </div>
    </div>

    {/* Terminal Command Simulation */}
    <div className="bg-black/50 rounded-lg border border-white/10 p-4 font-mono text-sm shadow-xl">
      <div className="flex gap-2 text-gray-500 mb-2">
        <div className="w-3 h-3 rounded-full bg-red-500/50" />
        <div className="w-3 h-3 rounded-full bg-yellow-500/50" />
        <div className="w-3 h-3 rounded-full bg-green-500/50" />
      </div>
      <div className="text-green-400 flex gap-2">
        <span>➜</span>
        <span className="text-cyan-400">~</span>
        <span className="text-white">./init_portfolio.sh --interactive</span>
      </div>
      <div className="text-gray-400 mt-2 space-y-1">
        <div>{'>'} Initializing AI Assistant... <span className="text-green-400">[OK]</span></div>
        <div>{'>'} Loading Modules... <span className="text-green-400">[OK]</span></div>
        <div className="animate-pulse">{'>'} System Ready_</div>
      </div>
    </div>
  </div>
);

const ProjectsContent = () => {
  const projects = [
    {
      id: 1,
      name: "Microsoft Fabric",
      type: "Data Platform",
      tech: ["React", "AI", "Azure"],
      status: "Production",
      description: "Next-generation data analytics platform integrating AI copilot."
    },
    {
      id: 2,
      name: "Azure Data",
      type: "Cloud Infrastructure",
      tech: ["Design Systems", "Cloud"],
      status: "Live",
      description: "Unified developer experience for Azure data services."
    },
    {
      id: 3,
      name: "AI Copilot Integration",
      type: "AI/ML",
      tech: ["LLM", "UX Research"],
      status: "Beta",
      description: "Context-aware AI assistance for data engineers."
    }
  ];

  return (
    <div className="font-mono">
      <div className="text-gray-500 mb-4">{'// Loaded from API: /v1/projects'}</div>
      <div className="text-purple-400">const <span className="text-yellow-400">projects</span> = [</div>
      
      <div className="pl-4 space-y-6 my-2">
        {projects.map((p, i) => (
           <div key={i} className="relative group">
             {/* Card Overlay on Hover */}
             <div className="hidden group-hover:block absolute -right-4 -top-4 w-64 p-4 bg-[#1a1a1c] border border-cyan-500/30 rounded-lg shadow-2xl z-20 animate-fade-in pointer-events-none">
                <div className="h-24 bg-gradient-to-br from-cyan-900/40 to-purple-900/40 rounded mb-2 border border-white/5" />
                <h4 className="text-white font-bold">{p.name}</h4>
                <p className="text-xs text-gray-400 mt-1">{p.description}</p>
             </div>

             <div className="text-yellow-400">{'{'}</div>
             <div className="pl-4 border-l border-white/5 hover:border-white/20 transition-colors">
               <div><span className="text-cyan-300">"id"</span>: <span className="text-blue-300">{p.id}</span>,</div>
               <div><span className="text-cyan-300">"name"</span>: <span className="text-green-300">"{p.name}"</span>,</div>
               <div><span className="text-cyan-300">"type"</span>: <span className="text-green-300">"{p.type}"</span>,</div>
               <div><span className="text-cyan-300">"tech"</span>: [<span className="text-orange-300">{p.tech.map(t => `"${t}"`).join(', ')}</span>],</div>
               <div><span className="text-cyan-300">"status"</span>: <span className="text-green-300">"{p.status}"</span></div>
             </div>
             <div className="text-yellow-400">{'}'}{i < projects.length - 1 ? ',' : ''}</div>
           </div>
        ))}
      </div>
      
      <div className="text-purple-400">];</div>
    </div>
  );
};

const AboutContent = () => (
  <div className="font-mono text-sm md:text-base">
    <div className="text-gray-500 mb-6">{'/**\n * Configuration for Developer Profile\n * @author Fei Design\n */'}</div>
    
    <div>
      <span className="text-purple-400">interface</span> <span className="text-yellow-400">DeveloperProfile</span> {'{'}
    </div>
    
    <div className="pl-4 border-l border-gray-800 ml-1">
        <div className="py-1"><span className="text-cyan-300">name</span>: <span className="text-blue-400">string</span>;</div>
        <div className="py-1"><span className="text-cyan-300">role</span>: <span className="text-blue-400">"Manager"</span> | <span className="text-blue-400">"Designer"</span>;</div>
        <div className="py-1"><span className="text-cyan-300">company</span>: <span className="text-blue-400">string</span>;</div>
        <div className="py-1"><span className="text-cyan-300">focus</span>: <span className="text-blue-400">string[]</span>;</div>
    </div>

    <div className="text-yellow-400 mb-8">{'}'}</div>

    <div>
      <span className="text-purple-400">export const</span> <span className="text-blue-400">me</span>: <span className="text-yellow-400">DeveloperProfile</span> = {'{'}
    </div>

    <div className="pl-4 border-l border-gray-800 ml-1 space-y-1">
        <div><span className="text-cyan-300">name</span>: <span className="text-green-300">"Fei"</span>,</div>
        <div>
           <span className="text-cyan-300">role</span>: <span className="text-green-300">"Principal Design Manager"</span>,
        </div>
        <div><span className="text-cyan-300">company</span>: <span className="text-green-300">"Microsoft"</span>,</div>
        <div><span className="text-cyan-300">focus</span>: [</div>
        <div className="pl-4 text-orange-300">
          "Azure Data Group", "Microsoft Fabric", "AI Developer Experience"
        </div>
        <div>],</div>
        <div><span className="text-cyan-300">location</span>: <span className="text-green-300">"Redmond / Global"</span></div>
    </div>
    
    <div className="text-yellow-400">{'}'};</div>
  </div>
);

const ContactTerminal = () => {
  const [history, setHistory] = useState([
    { type: 'system', content: 'Contact Protocol Initiated...' },
    { type: 'system', content: 'Enter command: "email", "linkedin", "github" or message' }
  ]);
  const [input, setInput] = useState('');
  const bottomRef = useRef(null);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [history]);

  const handleCommand = (e) => {
    if (e.key === 'Enter') {
      const cmd = input.trim().toLowerCase();
      const newHistory = [...history, { type: 'user', content: input }];
      
      let response = '';
      if (cmd === 'email') response = 'Opening mail client...';
      else if (cmd === 'github') response = 'Navigating to github...';
      else if (cmd === 'clear') {
        setHistory([]); 
        setInput('');
        return;
      }
      else response = `Command '${cmd}' received. Message logged to server.`;

      setTimeout(() => {
        setHistory(prev => [...prev, { type: 'system', content: response }]);
      }, 300);
      
      setInput('');
    }
  };

  return (
    <div className="w-full max-w-2xl bg-black rounded-lg border border-white/10 p-6 font-mono text-sm shadow-2xl">
      <div className="flex gap-2 mb-4 text-gray-500">
         <Terminal size={14} />
         <span>bash — 80x24</span>
      </div>
      <div className="space-y-2 mb-4 h-64 overflow-y-auto custom-scrollbar">
        {history.map((line, i) => (
          <div key={i} className={`${line.type === 'user' ? 'text-white' : 'text-green-400'}`}>
            <span className="opacity-50 mr-2">{line.type === 'user' ? '$' : '>'}</span>
            {line.content}
          </div>
        ))}
        <div ref={bottomRef} />
      </div>
      <div className="flex items-center gap-2 text-white border-t border-white/10 pt-4">
        <span className="text-cyan-400 font-bold">➜</span>
        <input 
          type="text" 
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={handleCommand}
          className="bg-transparent border-none outline-none flex-1 focus:ring-0"
          placeholder="Type 'email' or send a message..."
          autoFocus
        />
      </div>
    </div>
  );
};


// --- AI SIDEBAR COMPONENT (GEMINI ENABLED) ---

const AIChatInterface = () => {
  const [messages, setMessages] = useState([
    { role: 'system', content: 'Greetings. I am the neural interface for this portfolio. I can answer questions about Fei\'s work at Microsoft.' }
  ]);
  const [input, setInput] = useState('');
  const [isTyping, setIsTyping] = useState(false);
  const scrollRef = useRef(null);

  useEffect(() => {
    if (scrollRef.current) {
      scrollRef.current.scrollTop = scrollRef.current.scrollHeight;
    }
  }, [messages, isTyping]);

  const handleSend = async () => {
    if (!input.trim()) return;
    const userMsg = input;
    setMessages(prev => [...prev, { role: 'user', content: userMsg }]);
    setInput('');
    setIsTyping(true);

    try {
      const apiKey = ""; 
      const response = await fetch(
        `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`,
        {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            contents: [{ parts: [{ text: userMsg }] }]
          }),
        }
      );

      if (!response.ok) throw new Error('API Error');

      const data = await response.json();
      const aiText = data.candidates?.[0]?.content?.parts?.[0]?.text || "I couldn't generate a response.";
      
      setMessages(prev => [...prev, { role: 'system', content: aiText }]);
    } catch (error) {
      console.error(error);
      setMessages(prev => [...prev, { role: 'system', content: "Error: Could not connect to the neural network." }]);
    } finally {
      setIsTyping(false);
    }
  };

  return (
    <div className="flex flex-col h-full bg-[#0c0c0e]">
       <div className="flex-1 overflow-y-auto p-4 space-y-6" ref={scrollRef}>
          {messages.map((msg, idx) => (
            <div key={idx} className={`text-sm leading-relaxed ${msg.role === 'user' ? 'text-right' : 'text-left'}`}>
              <div className={`inline-block p-3 rounded-lg max-w-[90%] ${msg.role === 'user' ? 'bg-[#1e1e1e] text-gray-200 border border-white/5' : 'text-gray-400'}`}>
                {msg.role === 'system' && <div className="text-xs font-bold text-cyan-500 mb-1 flex items-center gap-2"><Sparkles size={10} /> NOVA</div>}
                {msg.content}
              </div>
            </div>
          ))}
          {isTyping && (
             <div className="text-left">
               <span className="text-xs text-cyan-500 animate-pulse">Nova is thinking...</span>
             </div>
          )}
       </div>

       {/* Quick Actions */}
       <div className="px-4 py-2 flex flex-wrap gap-2 border-t border-white/5 bg-[#09090b]">
          <button onClick={() => setInput('What does Fei do?')} className="text-[10px] px-2 py-1 bg-[#1e1e1e] border border-white/10 rounded hover:border-cyan-500 hover:text-cyan-400 transition-colors">Role</button>
          <button onClick={() => setInput('Tell me about Microsoft Fabric')} className="text-[10px] px-2 py-1 bg-[#1e1e1e] border border-white/10 rounded hover:border-cyan-500 hover:text-cyan-400 transition-colors">Fabric</button>
       </div>

       {/* Input */}
       <div className="p-4 bg-[#09090b]">
         <div className="relative">
            <input 
              type="text" 
              value={input}
              onChange={(e) => setInput(e.target.value)}
              onKeyDown={(e) => e.key === 'Enter' && handleSend()}
              placeholder="Ask anything or cmd+k..."
              className="w-full bg-[#1e1e1e] text-white text-sm rounded-md pl-3 pr-10 py-2.5 focus:outline-none focus:ring-1 focus:ring-cyan-500/50 border border-white/5 placeholder:text-gray-600"
            />
            <button 
              onClick={handleSend}
              className="absolute right-2 top-2 p-1 text-gray-500 hover:text-white"
            >
              <Send size={14} />
            </button>
         </div>
         <div className="flex justify-between mt-2 text-[10px] text-gray-600 font-mono">
            <span>Powered by Gemini 2.5</span>
            <span>Ln 14, Col 32</span>
         </div>
       </div>
    </div>
  );
};

export default Portfolio;